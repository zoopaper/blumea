<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.blumea.cms.dao.IUserDao">
    <sql id="allAttribute">
        <![CDATA[
            id,
            account,
            password,
            email,
            create_time,
            activation,
            status
        ]]>
    </sql>
    <resultMap id="UserMap" type="User">
        <id column="id" property="id"/>
        <result column="account" property="account"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="create_time" property="createTime"/>
        <result column="activation" property="activation"/>
        <result column="status" property="status"/>
    </resultMap>

    <insert id="save"  useGeneratedKeys="true" keyProperty="id">
        insert into ms_user(
            account,
            password,
            email,
            create_time
            )
          VALUES(
            #{account},
            #{password},
            #{email},
            #{createTime}
            )
    </insert>

    <select id="getUserByAccount" parameterType="String" resultMap="UserMap">
        select
        <include refid="allAttribute"/>
        from ms_user where account=#{account} and status = 0
    </select>

    <select id="get"  resultMap="UserMap">
        select
        <include refid="allAttribute"/>
        from ms_user
        where id = #{id} and status = 0;
    </select>

    <update id="update">
      update ms_user set
          account=#{account},
          password=#{password},
          email=#{email},
      where id = #{id}
    </update>

    <update id="delete">
        update ms_user set status = 1 where id = #{id}
    </update>

    <update id="delteUserBatch" parameterType="string">
        update ms_user set status = 1 where FIND_IN_SET(id,#{0})
    </update>

    <select id="getUserTotalNum" resultType="int">
        select count(id) from ms_user
        where status =0
        and (#{0}='' or account like "%"#{0}"%");
    </select>

    <select id="getUserWithPage" resultType="org.blumea.cms.entity.UserEntity">
        select
        <include refid="allAttribute"/>
        from ms_user
        where isDel=0
        and (#{0}='' or userName like "%"#{0}"%")
        order by if(updateDate is null,createDate,updateDate) desc
        limit #{1},#{2};
    </select>

</mapper>