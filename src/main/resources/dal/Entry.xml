<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.stream.dao.IEntryDao">
    <sql id="allAttribute">
        <![CDATA[


            id,
            title,
            subhead,
            content,
            author,
            editor,
            dutyEditor,
            url,
            keyword,
            tag,
            summary,
            channelId,
            subjectId,
            category,
            isDel,
            status,
            ctime,
            utime



        ]]>
    </sql>
    <insert id="save" parameterType="org.stream.entity.EntryBean" useGeneratedKeys="true" keyProperty="id">
        insert into t_entry(name,funcId,description,createTime)
        values(#{name},#{funcId},#{description},#{createTime});
    </insert>

    <select id="get" parameterType="long" resultType="org.stream.entity.EntryBean">
        select a.id, a.name,a.description,a.funcId,a.createTime
        from t_entry a
        where id=#{0};
    </select>

    <update id="update" parameterType="org.stream.entity.EntryBean">
      update t_entry set
      name=#{name},
      description=#{description},
      funcId=#{funcId}
      where id=#{id}
    </update>

    <update id="delete" parameterType="org.stream.entity.EntryBean">
        update t_entry set isDel =1 where id =#{id}
    </update>

    <select id="getEntryTotalNum" resultType="int">
        select count(id) from t_entry
        where  (#{0}='' or title like "%"#{0}"%");
    </select>

    <select id="getEntryWithPage" resultType="org.stream.entity.EntryBean">
        select
        <include refid="allAttribute"/>
        from t_entry a
        where
        #{0}='' or title like "%"#{0}"%"
        order by ctime desc
        limit #{1},#{2};
    </select>

</mapper>